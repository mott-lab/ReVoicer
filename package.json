{
  "name": "pdf-converser-desktop",
  "version": "0.1.0",
  "description": "PDF Converser desktop app (Electron)",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "proxy:build": "cd vendor/claude-max-api-proxy && npm install && npm run build",
    "fetch-vosk-model": "node scripts/download-vosk-model.js",
    "postinstall": "node scripts/download-vosk-model.js && node scripts/setup-proxy.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "openai": "^4.70.0",
    "vosk-browser": "^0.0.8"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
